name: Build
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: godebos/debos:main
    steps:
      - name: Start Build
        uses: actions/checkout@v3
        run: |
          debos debian-ostree.yaml